# 32-bit CPU emulator
emulator file is binary ELF executable <br><br>

Example 'assembler' codes \*.i and compiled bytecode \*.bc can be found in asm folder<br>
Run as:<br>
  ./emulator run <bytecode_file><br>
  ./emulator trace <bytecode_file><br>
